#!/bin/bash

npm install  --save dotenv ejs mssql node-cron nodemailer nodemon concurrently

npm install  --save-dev @types/dotenv @types/ejs @types/mssql @types/node-cron @types/nodemailer @types/nodemon @types/concurrently

echo "Packages installed successfully!"