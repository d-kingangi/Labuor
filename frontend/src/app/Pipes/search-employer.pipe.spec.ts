import { SearchEmployerPipe } from './search-employer.pipe';

describe('SearchEmployerPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchEmployerPipe();
    expect(pipe).toBeTruthy();
  });
});
