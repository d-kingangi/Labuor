import { SearchTalentPipe } from './search-talent.pipe';

describe('SearchTalentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchTalentPipe();
    expect(pipe).toBeTruthy();
  });
});
